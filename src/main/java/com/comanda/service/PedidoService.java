package com.comanda.service;

import javax.servlet.http.HttpServletRequest;

import com.comanda.model.Pagamento;

public interface PedidoService {
	public String pagamento(Pagamento pagamento);
}
